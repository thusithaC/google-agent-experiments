# Core dependencies
# Add your project dependencies here

# Development dependencies (optional)
# Install with: pip install -r requirements.txt
# Or install dev dependencies with: pip install -e .[dev]

# For now, no external dependencies are required
# Add dependencies as needed, for example:
# requests>=2.28.0
# numpy>=1.24.0
# pandas>=2.0.0
